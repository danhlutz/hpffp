# chapter exercises

```
1) The Eq class
c) makes equality tests possible

2) The typeclass Ord
a) allows two values to be compared and
c) is a superclass of Eq

3) What is the typeclass of >
a) (>) :: Ord a => a -> a -> Bool

4) In x = divMod 16 12
c) the type of x is a tuple

5) The typeclass Integral includes
a) Int and Integer numbers
```
